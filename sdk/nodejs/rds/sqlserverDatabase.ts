// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Manages RDS SQLServer database resource within SberCloud.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as sbercloud from "@pulumi/sbercloud";
 *
 * const config = new pulumi.Config();
 * const instanceId = config.requireObject<any>("instanceId");
 * const test = new sbercloud.rds.SqlserverDatabase("test", {
 *     instanceId: instanceId,
 *     name: "test",
 * });
 * ```
 *
 * ## Import
 *
 * The RDS sqlserver database can be imported using the `instance_id` and `name` separated by a slash, e.g.
 *
 * bash
 *
 * ```sh
 * $ pulumi import sbercloud:Rds/sqlserverDatabase:SqlserverDatabase test <instance_id>/<name>
 * ```
 */
export class SqlserverDatabase extends pulumi.CustomResource {
    /**
     * Get an existing SqlserverDatabase resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SqlserverDatabaseState, opts?: pulumi.CustomResourceOptions): SqlserverDatabase {
        return new SqlserverDatabase(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'sbercloud:Rds/sqlserverDatabase:SqlserverDatabase';

    /**
     * Returns true if the given object is an instance of SqlserverDatabase.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SqlserverDatabase {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SqlserverDatabase.__pulumiType;
    }

    /**
     * Indicates the character set used by the database.
     */
    declare public /*out*/ readonly characterSet: pulumi.Output<string>;
    /**
     * Specifies the ID of the RDS SQLServer instance.
     *
     * Changing this parameter will create a new resource.
     */
    declare public readonly instanceId: pulumi.Output<string>;
    /**
     * Specifies the database name. The database name can contain 1 to 64 characters,
     * and can include letters, digits, hyphens (-), underscores (_), and periods (.). It cannot start or end with an RDS for
     * SQL Server system database name. RDS for SQL Server system databases include **master**, **msdb**, **model**,
     * **tempdb**, **resource**, and **rdsadmin**.
     *
     * Changing this parameter will create a new resource.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Specifies the region in which to create the resource.
     * If omitted, the provider-level region will be used. Changing this parameter will create a new resource.
     */
    declare public readonly region: pulumi.Output<string>;
    /**
     * Indicates the database status. Its value can be any of the following:
     * + **Creating**: The database is being created.
     * + **Running**: The database is running.
     * + **Deleting**: The database is being deleted.
     * + **Not Exists**: The database does not exist.
     */
    declare public /*out*/ readonly state: pulumi.Output<string>;

    /**
     * Create a SqlserverDatabase resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SqlserverDatabaseArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SqlserverDatabaseArgs | SqlserverDatabaseState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SqlserverDatabaseState | undefined;
            resourceInputs["characterSet"] = state?.characterSet;
            resourceInputs["instanceId"] = state?.instanceId;
            resourceInputs["name"] = state?.name;
            resourceInputs["region"] = state?.region;
            resourceInputs["state"] = state?.state;
        } else {
            const args = argsOrState as SqlserverDatabaseArgs | undefined;
            if (args?.instanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceId'");
            }
            resourceInputs["instanceId"] = args?.instanceId;
            resourceInputs["name"] = args?.name;
            resourceInputs["region"] = args?.region;
            resourceInputs["characterSet"] = undefined /*out*/;
            resourceInputs["state"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SqlserverDatabase.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SqlserverDatabase resources.
 */
export interface SqlserverDatabaseState {
    /**
     * Indicates the character set used by the database.
     */
    characterSet?: pulumi.Input<string>;
    /**
     * Specifies the ID of the RDS SQLServer instance.
     *
     * Changing this parameter will create a new resource.
     */
    instanceId?: pulumi.Input<string>;
    /**
     * Specifies the database name. The database name can contain 1 to 64 characters,
     * and can include letters, digits, hyphens (-), underscores (_), and periods (.). It cannot start or end with an RDS for
     * SQL Server system database name. RDS for SQL Server system databases include **master**, **msdb**, **model**,
     * **tempdb**, **resource**, and **rdsadmin**.
     *
     * Changing this parameter will create a new resource.
     */
    name?: pulumi.Input<string>;
    /**
     * Specifies the region in which to create the resource.
     * If omitted, the provider-level region will be used. Changing this parameter will create a new resource.
     */
    region?: pulumi.Input<string>;
    /**
     * Indicates the database status. Its value can be any of the following:
     * + **Creating**: The database is being created.
     * + **Running**: The database is running.
     * + **Deleting**: The database is being deleted.
     * + **Not Exists**: The database does not exist.
     */
    state?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a SqlserverDatabase resource.
 */
export interface SqlserverDatabaseArgs {
    /**
     * Specifies the ID of the RDS SQLServer instance.
     *
     * Changing this parameter will create a new resource.
     */
    instanceId: pulumi.Input<string>;
    /**
     * Specifies the database name. The database name can contain 1 to 64 characters,
     * and can include letters, digits, hyphens (-), underscores (_), and periods (.). It cannot start or end with an RDS for
     * SQL Server system database name. RDS for SQL Server system databases include **master**, **msdb**, **model**,
     * **tempdb**, **resource**, and **rdsadmin**.
     *
     * Changing this parameter will create a new resource.
     */
    name?: pulumi.Input<string>;
    /**
     * Specifies the region in which to create the resource.
     * If omitted, the provider-level region will be used. Changing this parameter will create a new resource.
     */
    region?: pulumi.Input<string>;
}
